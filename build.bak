<target name="compile" depends="" description="compile the landscape source files">
    <javac srcdir="." destdir="${build}">
        <classpath>
            <fileset dir="${lib}">
                <include name="lib/*.jar" />
            </fileset>
            <fileset dir="${test_lig}">
                <include name="lib/*.jar" />
            </fileset>
        </classpath>
    </javac>
